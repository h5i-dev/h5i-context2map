{"id":"ffcc4b6feee02b26","kind":"tool-output","cmd":"bash -c ls .github/workflows/ && cat .github/workflows/release.yml 2>/dev/null | head -60; git log --oneline main..fix-overlap; git status --short | head; grep -m2 version Cargo.toml crates/ctx2img-cli/Cargo.toml 2>/dev/null","cwd":"/home/koukyosyumei/Dev/h5i-context2map","exit_code":0,"git_tree":"7e2758ff03ee0f11e68cb7613b19edaa93ce387e","branch":"main","timestamp":"2026-07-17T13:29:14.762386Z","raw_oid":"sha256:ffcc4b6feee02b261b2202c7a11997fcb79574999dc1dc2a2302fbe2013f0702","raw_size":2298,"raw_lines":68,"filter_version":1,"summary":"ci.yml\nrelease.yml\nname: Release\n\non:\n  push:\n    tags: [\"v*\"]\n\npermissions:\n  contents: write\n\nenv:\n  CARGO_TERM_COLOR: always\n\njobs:\n  build:\n    name: build (${{ matrix.target }})\n    runs-on: ${{ matrix.os }}\n    strategy:\n      fail-fast: false\n      matrix:\n        include:\n          - os: ubuntu-latest\n            target: x86_64-unknown-linux-gnu\n          - os: macos-13\n            target: x86_64-apple-darwin\n          - os: macos-latest\n            target: aarch64-apple-darwin\n          - os: windows-latest\n            target: x86_64-pc-windows-msvc\n    steps:\n      - uses: actions/checkout@v4\n      - uses: dtolnay/rust-toolchain@stable\n        with:\n          targets: ${{ matrix.target }}\n      - uses: Swatinem/rust-cache@v2\n      - name: Build\n        run: cargo build --release -p ctx2img-cli --target ${{ matrix.target }}\n      - name: Package (unix)\n        if: runner.os != 'Windows'\n        run: |\n          staging=\"ctx2img-${{ github.ref_name }}-${{ matrix.target }}\"\n          mkdir -p \"$staging\"\n          cp \"target/${{ matrix.target }}/release/ctx2img\" \"$staging/\"\n          cp README.md LICENSE \"$staging/\"\n          cp -r skills \"$staging/skills\"\n          tar czf \"$staging.tar.gz\" \"$staging\"\n          echo \"ASSET=$staging.tar.gz\" >> \"$GITHUB_ENV\"\n      - name: Package (windows)\n        if: runner.os == 'Windows'\n        shell: pwsh\n        run: |\n          $staging = \"ctx2img-${{ github.ref_name }}-${{ matrix.target }}\"\n          New-Item -ItemType Directory -Path $staging | Out-Null\n          Copy-Item \"target/${{ matrix.target }}/release/ctx2img.exe\" $staging/\n          Copy-Item README.md,LICENSE $staging/\n          Copy-Item -Recurse skills \"$staging/skills\"\n          Compress-Archive -Path $staging -DestinationPath \"$staging.zip\"\n          echo \"ASSET=$staging.zip\" | Out-File -FilePath $env:GITHUB_ENV -Append\n      - uses: actions/upload-artifact@v4\n        with:\n          name: ${{ matrix.target }}\n710e95e Shrink section-map pages to their content: no more white-padded boxes\n80d9fe3 Apply rustfmt to handle-only label position\n928af88 Never paint cell text over neighboring boxes in section maps\nCargo.toml:version = \"0.1.0\"\nCargo.toml:serde = { version = \"1\", features = [\"derive\"] }\ncrates/ctx2img-cli/Cargo.toml:version.workspace = true","store":"local","codec":"none","raw_tokens":634,"summary_tokens":633,"structured":{"schema_version":1,"tool":"bash","kind":"generic","status":"ok","exit_code":0,"parser_confidence":"generic","raw_oid":"sha256:ffcc4b6feee02b261b2202c7a11997fcb79574999dc1dc2a2302fbe2013f0702","truncated":{"findings_total":0,"findings_shown":0,"raw":false},"body":"ci.yml\nrelease.yml\nname: Release\n\non:\n  push:\n    tags: [\"v*\"]\n\npermissions:\n  contents: write\n\nenv:\n  CARGO_TERM_COLOR: always\n\njobs:\n  build:\n    name: build (${{ matrix.target }})\n    runs-on: ${{ matrix.os }}\n    strategy:\n      fail-fast: false\n      matrix:\n        include:\n          - os: ubuntu-latest\n            target: x86_64-unknown-linux-gnu\n          - os: macos-13\n            target: x86_64-apple-darwin\n          - os: macos-latest\n            target: aarch64-apple-darwin\n          - os: windows-latest\n            target: x86_64-pc-windows-msvc\n    steps:\n      - uses: actions/checkout@v4\n      - uses: dtolnay/rust-toolchain@stable\n        with:\n          targets: ${{ matrix.target }}\n      - uses: Swatinem/rust-cache@v2\n      - name: Build\n        run: cargo build --release -p ctx2img-cli --target ${{ matrix.target }}\n      - name: Package (unix)\n        if: runner.os != 'Windows'\n        run: |\n          staging=\"ctx2img-${{ github.ref_name }}-${{ matrix.target }}\"\n          mkdir -p \"$staging\"\n          cp \"target/${{ matrix.target }}/release/ctx2img\" \"$staging/\"\n          cp README.md LICENSE \"$staging/\"\n          cp -r skills \"$staging/skills\"\n          tar czf \"$staging.tar.gz\" \"$staging\"\n          echo \"ASSET=$staging.tar.gz\" >> \"$GITHUB_ENV\"\n      - name: Package (windows)\n        if: runner.os == 'Windows'\n        shell: pwsh\n        run: |\n          $staging = \"ctx2img-${{ github.ref_name }}-${{ matrix.target }}\"\n          New-Item -ItemType Directory -Path $staging | Out-Null\n          Copy-Item \"target/${{ matrix.target }}/release/ctx2img.exe\" $staging/\n          Copy-Item README.md,LICENSE $staging/\n          Copy-Item -Recurse skills \"$staging/skills\"\n          Compress-Archive -Path $staging -DestinationPath \"$staging.zip\"\n          echo \"ASSET=$staging.zip\" | Out-File -FilePath $env:GITHUB_ENV -Append\n      - uses: actions/upload-artifact@v4\n        with:\n          name: ${{ matrix.target }}\n710e95e Shrink section-map pages to their content: no more white-padded boxes\n80d9fe3 Apply rustfmt to handle-only label position\n928af88 Never paint cell text over neighboring boxes in section maps\nCargo.toml:version = \"0.1.0\"\nCargo.toml:serde = { version = \"1\", features = [\"derive\"] }\ncrates/ctx2img-cli/Cargo.toml:version.workspace = true"},"action":"read"}
